{
  "name": "nexus-park",
  "private": true,
  "version": "0.1.0",
  "description": "Nexus Park — a persistent multiplayer 3D metaverse for the browser",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm:dev -w server\" \"npm:dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "db:setup": "npm run db:setup -w server",
    "db:reset": "npm run db:reset -w server",
    "test": "npm run test -w shared && npm run test -w server",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
