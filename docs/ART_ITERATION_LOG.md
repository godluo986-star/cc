# 持续精修迭代日志

> 自主持续精修模式的轮次记录。中断后从此文件恢复,不重扫已完成项。

## 2026-07-26 · 轮次 1 —— 一起看电影(最高优先级)核心架构

**版本**:`650e0d3`(Wave2 黄昏街区)+ `23dc92f`(watch party 核心),均已推送。

**本轮主题**:单实例播放层 + 分层漂移纠偏(任务书第一/二阶段)。

**完成**:
- 全客户端唯一持久播放器(MediaLayer):世界内 = drei 同款 CSS3D 矩阵逐帧直写 DOM;全屏 = 同一批节点换样式。进出全屏 ×10 实测同一 iframe 实例存活、零重建、零双声(`scripts/watch-test.mjs` 8/8)。
- 分层纠偏(SYNC 配置集中):死区 0.18s / 变速 ±6% 不变调至 0.8s / 冷却单次 seek 至 2s / 强制重同步;本地缓冲不 seek 轰炸、不暂停别人;YT 走 seek 档 + 长冷却。
- 服务器媒体状态单调 revision;后加入者拿到一致 {url, kind, revision}。
- 多屏镜像按宽度认领画面(房间双电视:大屏出画,小屏暗面)。
- 黑边感知 contentRect 映射 + `np_watch_debug=1` 漂移调试角标。

**同轮合入(Wave2)**:黄昏→夜光照重标定(夜可读)、克制后期栈+玩家轮廓光、4 个异常点、阶段式加载屏、网吧 NEXUS(机位屏放本人共享流)+ 雀庄东风阁室内、日麻 3D 桌 + 雀魂式面板(双客户端实测,旁观脱敏)。

**门禁**:typecheck×3 零错;shared 108 / server 49 / client 7 测试;sync-test 11/11(双端漂移 0.08s);smoke 11/11;watch-test 8/8;console 0 错。

**遗留 / 下一轮(按任务书 §三十四 阶段序)**:
1. **观影阶段 3**:后台标签页恢复重同步(visibilitychange → 重校时+一次平滑追赶)、需要手势时的「点击继续一起观看」、房主断线宽限与控制权转移(目前公共屏人人可控/房间按房主策略,还没有 HOST_AND_COHOST/REQUEST 模式)。
2. **观影阶段 4**:入座自动朝向银幕、开播灯光变暗、观看成员名单、音量四分类(媒体/语音/环境/UI,现有媒体+语音+主音量三档)、共享进度条的拖动预览节流(现面板 SeekBar 已有,但未做拖动期间只发最终位)。
3. **观影阶段 5**:5/10 人规模压测、真实视频源的漂移 P95 采样(沙盒无外网,只能量化到状态层;需线上环境补真实播放采样)。
4. 其他排队任务(用户指令序):媒体点击坐标网络化(观看/控制双模式)、温馨生活空间改造方案文档+第一阶段、物理抓取第一阶段+第一人称、`public/assets/generated/` 素材管线与 `docs/GENERATED_ASSETS.md`。

**已知诚实备注**:雀魂面板的吃(chi)在福州麻将不存在、日麻已实现;日麻规则简化清单在 `shared/src/riichi/` 模块头。

## 2026-07-26 · 轮次 2 —— 观影阶段 3+4(恢复语义 + 影厅氛围)

**主题**:真全屏 / 后台恢复 / 手势恢复 / 影院灯暗 / 观看成员。

**完成**:
- 真 `requestFullscreen`:对覆盖层容器调用,失败静默降级为页面内覆盖层(同一棵播放器树,降级不重建实例);浏览器侧退出(Esc/手势)经 `fullscreenchange` 收敛状态。watch-test 的 ×10 循环现在走的就是原生全屏路径。
- 后台标签页恢复:`visibilitychange` → 立即执行一次追齐(video 与 YT 两个播放器都接了),回前台不再等下一个 500ms tick。
- 自动播放被浏览器拦截时:显示「▶ 点击继续一起观看」,点击后**先追到当前权威进度再播**,不从旧进度续播。
- 影院开播顶灯平滑压到 22%(暂停 55%,无片全亮),灯罩自发光同步变暗;`DimmableCeilingLight` 按帧 lerp,不跳变。
- 媒体面板显示「👀 正在一起看(N 人):名单」。
- 进度条本来就是拖动预览+松手才提交,复核合规,未改。
- watch-test 改为轮询断言(原生全屏过渡会暂停渲染几百毫秒,定长等待会误报)。

**门禁**:typecheck 零错;shared 108 / server 49 / client 7;watch-test 8/8(原生全屏 ×10);sync-test 11/11;smoke 11/11;console 0 错。

**下一轮候选**(按任务书优先级):
1. 物理抓取第一阶段 + 第一人称视角(任务书"必须保留"清单里已把它当既有功能,当前尚不存在——最高缺口)。
2. 温馨生活空间改造方案文档 + 阶段一(静音/屏蔽、快速进入、手机触控)。
3. 观影阶段 5:5/10 人规模压测、真实视频源漂移 P95(需外网环境)。
4. 三维屏 Raycast UV 点击网络化(观看/控制双模式)。

## 2026-07-26 · 轮次 3 —— 团子即时物理抓取·第一阶段 + 第一人称

**主题**:任务书"必须保留"清单里的最后两个缺口。

**完成**:
- 服务器权威弹簧约束模拟(无物理引擎依赖,10Hz tick 积分):长按 G/左键按住即抓、松手即放;抓取点(相机→身体连线截断)决定侧倾;把持点随视角 pitch 抬高/压低,夹 [HOLD_MIN,HOLD_MAX]×[地面+0.2, 抓取者+2.2];重力/阻尼/最大力/最大速度全上限;位置过共享碰撞(不穿墙不穿地);距离>3.4 断裂、被抓者方向键挣扎 2.2s 挣脱;拖人时抓取者限速 55%;离场/断线/坠界自动释放+安全复位;全部走既有 snap 通道与中文 toast。
- 客户端:候选准星黄圈提示;被抓者本地预测停用、吸附服务器快照(hot.selfSnap),释放无跳变;被抓侧倾 ±0.5rad + 悬空拉伸;V 键第一/第三人称(0.25s 平滑过渡、第一人称隐藏自身、roll 恒 0);「减少镜头运动」设置(被抓时眼高变化减速)。
- 帮助/设置文案更新。

**门禁**:typecheck×3 零错;shared+client 123 / server 61(12 个抓取用例);双 WS e2e 重放 2/2(举高↑且≤上限、挣扎不脱、释放落地不穿地、超距拒抓);smoke 11/11;watch-test 8/8;console 0 错。

**下一轮候选**:①温馨空间改造方案 + 阶段一(静音/屏蔽/快速进入/手机触控);②抓取第二阶段(道具抓取/双人同抓/叠高纪念照);③观影 5/10 人压测;④屏幕点击坐标网络化。

## 2026-07-26 · 轮次 4 —— 温馨空间阶段一 a:个人边界(静音/屏蔽/免抓取)

**主题**:任务书 §八 安全与隐私的第一批硬能力。

**完成**:
- 新 `useSocial` store(localStorage `np_social`,按 userId 跨会话稳定):屏蔽名单、单独静音、单独音量(0–150%)。
- 在线名单点开任意玩家 → 音量滑条 / 🔇 静音 / 🚫 屏蔽;行内 🚫/🔇 徽标。
- 屏蔽 = 硬边界:聊天不显示;语音不建链(desiredPeers 过滤 + 信令丢弃);**服务器双向不中继 RTC 信令**;互相不能抓(服务器拒绝 + 客户端不作候选);提示语不点破屏蔽关系。
- 免抓取开关(设置面板):`prefs` 消息即时同步,服务器强制;连接/重连自动重发偏好。
- 新 c2s `prefs {noGrab?, blocked?[]}`;session 增 `blockedUsers: Set<userId>`。

**门禁**:typecheck×3 零错;shared+client 123 / server 65(新增 2 个边界用例:免抓取拒抓、屏蔽拒抓+双向信令不中继+解除恢复);smoke 11/11;console 0 错。

**下一轮候选**:①手机触控(虚拟摇杆+动作键,README 桌面限定要摘掉);②快速进入(昵称+颜色+麦克风三步游客流);③抓取第二阶段;④观影多人压测。
