# 持续精修迭代日志

> 自主持续精修模式的轮次记录。中断后从此文件恢复,不重扫已完成项。

## 2026-07-26 · 轮次 1 —— 一起看电影(最高优先级)核心架构

**版本**:`650e0d3`(Wave2 黄昏街区)+ `23dc92f`(watch party 核心),均已推送。

**本轮主题**:单实例播放层 + 分层漂移纠偏(任务书第一/二阶段)。

**完成**:
- 全客户端唯一持久播放器(MediaLayer):世界内 = drei 同款 CSS3D 矩阵逐帧直写 DOM;全屏 = 同一批节点换样式。进出全屏 ×10 实测同一 iframe 实例存活、零重建、零双声(`scripts/watch-test.mjs` 8/8)。
- 分层纠偏(SYNC 配置集中):死区 0.18s / 变速 ±6% 不变调至 0.8s / 冷却单次 seek 至 2s / 强制重同步;本地缓冲不 seek 轰炸、不暂停别人;YT 走 seek 档 + 长冷却。
- 服务器媒体状态单调 revision;后加入者拿到一致 {url, kind, revision}。
- 多屏镜像按宽度认领画面(房间双电视:大屏出画,小屏暗面)。
- 黑边感知 contentRect 映射 + `np_watch_debug=1` 漂移调试角标。

**同轮合入(Wave2)**:黄昏→夜光照重标定(夜可读)、克制后期栈+玩家轮廓光、4 个异常点、阶段式加载屏、网吧 NEXUS(机位屏放本人共享流)+ 雀庄东风阁室内、日麻 3D 桌 + 雀魂式面板(双客户端实测,旁观脱敏)。

**门禁**:typecheck×3 零错;shared 108 / server 49 / client 7 测试;sync-test 11/11(双端漂移 0.08s);smoke 11/11;watch-test 8/8;console 0 错。

**遗留 / 下一轮(按任务书 §三十四 阶段序)**:
1. **观影阶段 3**:后台标签页恢复重同步(visibilitychange → 重校时+一次平滑追赶)、需要手势时的「点击继续一起观看」、房主断线宽限与控制权转移(目前公共屏人人可控/房间按房主策略,还没有 HOST_AND_COHOST/REQUEST 模式)。
2. **观影阶段 4**:入座自动朝向银幕、开播灯光变暗、观看成员名单、音量四分类(媒体/语音/环境/UI,现有媒体+语音+主音量三档)、共享进度条的拖动预览节流(现面板 SeekBar 已有,但未做拖动期间只发最终位)。
3. **观影阶段 5**:5/10 人规模压测、真实视频源的漂移 P95 采样(沙盒无外网,只能量化到状态层;需线上环境补真实播放采样)。
4. 其他排队任务(用户指令序):媒体点击坐标网络化(观看/控制双模式)、温馨生活空间改造方案文档+第一阶段、物理抓取第一阶段+第一人称、`public/assets/generated/` 素材管线与 `docs/GENERATED_ASSETS.md`。

**已知诚实备注**:全屏用的是覆盖层(fixed inset:0)而非 `requestFullscreen()` API——下一轮把 FullscreenController 补上真全屏 + 能力检测降级;雀魂面板的吃(chi)在福州麻将不存在、日麻已实现;日麻规则简化清单在 `shared/src/riichi/` 模块头。
